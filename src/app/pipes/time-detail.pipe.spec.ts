import { TimeDetailPipe } from './time-detail.pipe';

describe('TimeDetailPipe', () => {
  it('create an instance', () => {
    const pipe = new TimeDetailPipe();
    expect(pipe).toBeTruthy();
  });
});
