import { ConvertHMPipe } from './convert-hm.pipe';

describe('ConvertHMPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertHMPipe();
    expect(pipe).toBeTruthy();
  });
});
