import { LinkMessageSnackBarComponent } from './link-message-snack-bar.component';

describe('LinkMessageSnackBarComponent', () => {
  it('should mount', () => {
    cy.mount(LinkMessageSnackBarComponent);
  });
});