import { TwoDigitsDirective } from './two-digits.directive';

describe('TwoDigitsDirective', () => {
  it('should create an instance', () => {
    // const directive = new TwoDigitsDirective();
    // expect(directive).toBeTruthy();
  });
});
